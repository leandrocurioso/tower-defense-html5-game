import { Scene } from "./scene";

export class UiScene extends Scene {

  constructor(options = { key: "ui" }) {
    super(options);
  }

  init() {
    super.init();
  }

  preload() {
  }

  create() {
    
  }

  update() {
  }

}
